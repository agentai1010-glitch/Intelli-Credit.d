[phases.setup]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-eng", "libsm6", "libxext6", "ghostscript", "poppler-utils", "python3-tk"]

[phases.build]
cmds = ["python -m pip install -r requirements.txt", "python -m spacy download en_core_web_sm"]
